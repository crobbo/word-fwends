<h1 class="font-bold text-4xl">Games#show</h1>

<div id="<%= dom_id(@game) %>"> 
  <%= render "game", :game => @game %>
</div>

<div class="flex flex-col justify-center max-w-sm mx-auto mt-5">
  <%= render "keyboard" %>
</div>