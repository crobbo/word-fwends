<div class="pt-20 text-center text-5xl font-bold text-white">
  <% if @game.over? %>
    You Win!
  <% end %>
</div>

<div id="<%= dom_id(@game) %>"> 
  <%= render "game", :game => @game %>
</div>

<div class="max-w-xl mx-auto mt-5">
  <%= render "keyboard", :game => @game %>
</div>