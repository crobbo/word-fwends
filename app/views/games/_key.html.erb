<%= tag.button id:"#{game.id}_key_#{letter}", data: {action: "keyboard#onscreenKey"}, 
    class: ["
            h-14 w-12 
            rounded-sm border 
            mx-1
            flex 
            justify-center
            items-center
            cursor-pointer
            text-center
            text-white
            text-sm", 
            "bg-gray-400 border-none": game.misses?(letter) && !game.matches?(letter) && !game.occurances?(letter),
            "bg-orange-400 border-none": game.occurances?(letter) && !game.matches?(letter),
            "bg-green-400 border-none": game.matches?(letter)  ] do %>
    <%= letter.upcase %>
<% end %>